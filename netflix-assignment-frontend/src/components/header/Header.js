import React from "react";
import { HeaderBar, MainIcon } from "./HeaderElements";

const Header = () => {
  return (
    <HeaderBar>
      <MainIcon size={50} />
    </HeaderBar>
  );
};

export default Header;
